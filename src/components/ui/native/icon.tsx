// Default / web Icon — Material icons via `@expo/vector-icons`.
// Metro picks `icon.ios.tsx` on iOS and `icon.android.tsx` on Android; this
// bare `icon.tsx` is the web and any-other-platform fallback.
export { MaterialIcon as Icon } from "./icon.shared";
